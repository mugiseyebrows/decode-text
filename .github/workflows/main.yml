name: action
on: [push]
jobs:
  task1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: "which python3"
      - run: "which qmake"
      - run: "which g++"